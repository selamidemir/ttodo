import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 5,
    backgroundColor: 'orange',
    fontSize: 35,
    textAlign: 'center',
    borderBottomWidth: 1,
    borderColor: 'black',
  },
});
